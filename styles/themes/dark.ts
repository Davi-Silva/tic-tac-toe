import { dark as game } from "./components/Game";
import { dark as buttons } from "./components/Buttons";
import { dark as square } from "./components/Square";
import { dark as display } from "./components/Display";
import { dark as leaderboard } from "./components/Leaderboard";
import { dark as global } from "./global";

export default {
  game,
  buttons,
  square,
  display,
  leaderboard,
  global,
};
