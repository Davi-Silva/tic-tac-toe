export interface IStyledButtons {
  reset: {
    background: string;
    borderColor: string;
    color: string;
  };
  themeSwitcher: {
    background: string;
    borderColor: string;
    toggler: {
      background: string;
    };
  };
}
